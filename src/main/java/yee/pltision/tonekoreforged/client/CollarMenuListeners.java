/*
package yee.pltision.tonekoreforged.client;

import net.minecraft.world.inventory.AbstractContainerMenu;
import net.minecraft.world.inventory.ContainerListener;
import net.minecraft.world.inventory.Slot;
import net.minecraft.world.item.ItemStack;
import net.minecraftforge.event.entity.player.PlayerContainerEvent;
import net.minecraftforge.eventbus.api.SubscribeEvent;
import net.minecraftforge.fml.common.Mod;

@Mod.EventBusSubscriber
public class CollarMenuListeners {

    @SubscribeEvent
    public static void addListener(PlayerContainerEvent.Open event){
        AbstractContainerMenu menu=event.getContainer()；
        Slot collarSlot=new Slot(event.getContainer()){

        };
        event.getContainer().slots.add()
        event.getContainer().slots.remove()
        event.getContainer().addSlotListener(new ContainerListener() {
            @Override
            public void slotChanged(AbstractContainerMenu p_39315_, int p_39316_, ItemStack p_39317_) {

            }

            @Override
            public void dataChanged(AbstractContainerMenu p_150524_, int p_150525_, int p_150526_) {

            }
        });
    }

}
*/
